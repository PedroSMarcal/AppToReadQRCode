import { styled } from "@stitches/react";

export const QRCodeStle = styled('div', {
    display: 'flex',
    justifyContent: 'center',
},
'p', {
    fontSize: 25,
    display: 'grid',
    justifyContent: 'center'
}) 